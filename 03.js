function formatName(firstName, lastName) {
    return `${firstName || ''}${lastName ? '' + lastName : ''}`;
}
// 此重構主要原因如下：

// 1.模板字串：使用模板字串能夠提高程式碼的可讀性，也使字串的組合更加直觀，更方便處理字串的組合。

// 2.條件運算子：使用條件運算子來簡化條件判斷的邏輯，使得在一行程式碼中處理兩種條件變得更加簡潔。

// 以上重構讓程式碼更簡潔、易讀，並保持原有的邏輯。